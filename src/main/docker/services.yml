# This configuration is intended for development purpose, it's **your** responsibility to harden it for production
name: coffechain
services:
  postgresql:
    extends:
      file: ./postgresql.yml
      service: postgresql
# Currently don't need kafka, when re-architecture we will need this.
#  kafka:
#    extends:
#      file: ./kafka.yml
#      service: kafka
